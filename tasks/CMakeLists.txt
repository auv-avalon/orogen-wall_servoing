# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(sonarvizkitTaskLib)
ADD_LIBRARY(${SONARVIZKIT_TASKLIB_NAME} SHARED 
    ${SONARVIZKIT_TASKLIB_SOURCES})


TARGET_LINK_LIBRARIES(${SONARVIZKIT_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${SONARVIZKIT_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SONARVIZKIT_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SONARVIZKIT_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SONARVIZKIT_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SONARVIZKIT_TASKLIB_HEADERS}
    DESTINATION include/orocos/sonarvizkit)

