# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(wall_servoingTaskLib)
ADD_LIBRARY(${WALL_SERVOING_TASKLIB_NAME} SHARED 
    ${WALL_SERVOING_TASKLIB_SOURCES})


TARGET_LINK_LIBRARIES(${WALL_SERVOING_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${WALL_SERVOING_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${WALL_SERVOING_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${WALL_SERVOING_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${WALL_SERVOING_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${WALL_SERVOING_TASKLIB_HEADERS}
    DESTINATION include/orocos/wall_servoing)

